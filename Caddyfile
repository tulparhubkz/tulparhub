# Replace tulparhub.kz with your real domain. Caddy fetches + renews TLS certs
# automatically via Let's Encrypt. Set an email below for expiry notices.
{
	email admin@tulparhub.kz
}

tulparhub.kz, www.tulparhub.kz {
	encode zstd gzip
	reverse_proxy app:3000
}
