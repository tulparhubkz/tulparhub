node_modules/
.next/
# Every .env variant carries secrets except the committed example.
.env*
!.env.example
*.log
.DS_Store
package-lock.json
backups/

# Vendor data exports (catalog/price dumps) — never commit
/data/
example_database.csv
*.csv